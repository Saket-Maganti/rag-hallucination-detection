# Legacy Chroma DBs

This folder holds local Chroma vector-store directories moved out of the
repository root during cleanup.

These files are preserved locally for reproducibility investigation, but they
are generated artifacts and should not be committed. Fresh experiment runs
should write new vector stores under `artifacts/generated/chroma_db*`.
