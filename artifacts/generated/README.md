# Generated Artifacts

This folder is for generated outputs that should not clutter the repository
root. Current local snapshots include HF dataset staging and HF Space deploy
staging moved during cleanup.

Future local Chroma vector stores should be generated as
`artifacts/generated/chroma_db*`.
