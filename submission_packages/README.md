# Submission Packages

Submission-facing material that is not itself an active paper source.

- `neurips/`: OpenReview metadata, checklist material, and local package zips.
- `arxiv/`: reserved for future arXiv packaging.

Active paper source lives under `papers/`.
